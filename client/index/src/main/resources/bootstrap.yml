spring:
  cloud:
    config:
      name: index,eureka,database,redis
      label: master
      discovery:
        enabled: true
        serviceId: config
  application:
    name: index #应用名称
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/